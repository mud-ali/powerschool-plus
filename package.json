{
  "name": "scheduler",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite", 
    "build": "vite build && cp out/** dist/ && cd dist && zip dist ./** && cd .. && mv dist/dist.zip . && rm -f ~/dist.zip && cp dist.zip ~/dist.zip",
    "static": "cp out/** dist/ && cd dist && zip dist ./** && cd .. && mv dist/dist.zip . && rm -f ~/dist.zip && cp dist.zip ~/dist.zip",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "chart.js": "^4.0.0",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.3",
    "vite": "^5.2.0"
    
  }

}
